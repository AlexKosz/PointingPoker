import React from 'react';

const VoteControls = () => {
	return (
		<section className='vote-controls'>
			<button className='reveal-votes'>Reveal Votes</button>
			<button className='clear-votes'>Clear Votes</button>
		</section>
	);
};

export default VoteControls;
